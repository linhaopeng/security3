1 将resources下的security.sql到数据库中执行。

2 可以直接将部署到tomcat。

3 项目访问及对的return 的关系
	 * http://localhost:9999/security3/account/accountAction!login.action
	 * 这个访问地址对应到  /WEB-INF/content/account/list.jsp
	 * 在  struts有相应的配置
	 
4 